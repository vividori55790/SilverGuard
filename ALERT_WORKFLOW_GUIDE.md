# SilverGuard 낙상 감지 알림 워크플로우 완전 가이드

## 📋 전체 프로세스 흐름

### 1️⃣ 낙상 감지 (core/engine.py)

- ST-GCN AI 모델이 낙상 패턴 인식
- 신뢰도 threshold 이상일 때 `_handle_detection()` 호출
- `_trigger_alert()` 메서드로 알림 프로세스 시작

### 2️⃣ 음성 확인 단계 (voice_module.py)

**`run_voice_emergency_check()` 함수가 실행됩니다:**

#### 1차 시도 (일반 볼륨)

```
🔊 "낙상이 감지되었습니다. 괜찮으십니까? 대답이 없으시면 구조 요청을 보냅니다."
⏱️ 10초간 음성 대기
```

**인식 가능한 응답:**

- ✅ **안전:** "괜찮아", "어", "나 안 다쳤어", "아무렇지 않아", "문제 없어"
  - → 시스템 정상 복귀, False Alarm으로 자동 분류
  - → 텔레그램/전화 **발송 안 함**
- 🚨 **긴급:** "아니", "아파", "도와줘", "살려줘", "병원", "119", "구조"
  - → 즉시 긴급 알림 전송
  - → Verified Fall로 자동 분류

#### 2차 시도 (무응답 시)

```
🔊 볼륨 100%로 자동 증가
🔊 "잘 안 들리실 수 있어 다시 크게 여쭤보겠습니다. 괜찮으신가요? 응답이 없으시면 비상 상황으로 간주합니다."
⏱️ 10초간 재대기
```

**최종 무응답 시:**

```
🔊 "응답이 전혀 없어 비상 상황으로 간주하고 구조 요청을 전송합니다."
→ 비상 상황으로 확정
```

### 3️⃣ 데이터 저장 (core/engine.py)

- 📸 **이미지:** `data/alert_images/FALL_YYYYMMDD_HHMMSS.jpg`
- 🎥 **영상:** `data/alert_images/FALL_VIDEO_YYYYMMDD_HHMMSS.mp4` (5초 버퍼)
- 💾 **학습 데이터:** `FALL_YYYYMMDD_HHMMSS.npy` (skeleton sequence)

### 4️⃣ 텔레그램 알림 전송 (utils.py)

**`send_telegram_alert()` 함수 실행:**

- 📸 사진 전송 (caption에 시간, 상황 포함)
- 🎥 영상 전송 (3~5초 영상)
- 🏥 인근 종합병원 정보 자동 첨부 (설정된 지역 기준)

**발송 조건:**

- 음성 체크 결과가 `"SAFE"`가 **아닐 때만** 전송
- 쿨다운 타임 (기본 60초) 적용

### 5️⃣ 자동 전화 발신 (utils.py)

**`make_phone_call()` 함수 실행:**

**조건:**

- ✅ 대시보드에서 "자동 전화 걸기" 활성화
- ✅ 긴급 연락처 입력됨
- ✅ Windows "휴대폰과 연결" 앱 설치됨

**동작 과정:**

1. `tel:` 프로토콜로 전화 앱 실행
2. 앱 윈도우 자동 최대화 및 포커싱
3. 전화번호 키패드 입력 (한 자씩)
4. Enter 키로 발신 실행

---

## 🔧 핵심 코드 구조

### voice_module.py

- `speak(text)` - PowerShell 기반 TTS (안정성 향상)
- `listen_and_analyze(timeout)` - 음성 인식 및 소리 분석
- `run_voice_emergency_check(image_path)` - 2단계 음성 확인 프로세스
- `set_max_volume()` - 시스템 볼륨 100% 강제 설정

### core/engine.py

- `_handle_detection()` - 낙상 감지 시 호출
- `_trigger_alert()` - 전체 알림 프로세스 오케스트레이션
- `_process_alert_async()` - 별도 스레드로 알림 실행 (UI 프리즈 방지)

### utils.py

- `send_telegram_alert(image, message, video)` - 텔레그램 전송
- `make_phone_call(number)` - PC 전화 앱 자동 실행
- `find_nearby_hospitals(region1, region2)` - 병원 정보 조회
- `move_alert_to_classified()` - 데이터 자동 분류

---

## ✅ 검증된 기능

### 음성 시스템

- ✅ PowerShell 기반 TTS (COM 충돌 해결)
- ✅ 2단계 음성 체크 (일반 볼륨 → 최대 볼륨)
- ✅ Google Speech API 음성 인식
- ✅ 안전 키워드 / 긴급 키워드 분류
- ✅ 백그라운드 구조 요청 감지

### 데이터 관리

- ✅ 이미지/영상/학습데이터 3종 세트 저장
- ✅ 음성 확인 결과에 따른 자동 분류
- ✅ 재학습을 위한 데이터 아카이빙

### 알림 시스템

- ✅ 텔레그램 사진 + 영상 전송
- ✅ 병원 정보 자동 첨부
- ✅ 오프라인 대기열 (인터넷 끊김 시)
- ✅ 쿨다운 타임으로 중복 알림 방지

### 전화 시스템

- ✅ Windows "휴대폰과 연결" 자동 실행
- ✅ 키패드 입력 자동화
- ✅ 윈도우 자동 최대화/포커싱

---

## 🚨 트러블슈팅

### 음성이 나오지 않을 때

**원인:** `subprocess` import 누락 (✅ 해결됨)
**증상:** speak() 함수에서 예외 발생
**해결:** voice_module.py 상단에 `import subprocess` 추가 완료

### 전화가 걸리지 않을 때

**원인:**

1. "휴대폰과 연결" 앱 미설치
2. 앱과 스마트폰 연결 안 됨
3. 긴급 연락처 미입력

**해결:**

1. Microsoft Store에서 "Phone Link" 설치
2. 앱에서 스마트폰 페어링
3. 대시보드에서 연락처 입력 및 "자동 전화" 활성화

### 텔레그램이 안 올 때

**체크리스트:**

1. 인터넷 연결 확인
2. 대시보드에서 토큰/챗ID 입력 확인
3. 봇과 1:1 대화 시작했는지 확인
4. 음성 체크에서 "괜찮아"를 말하지 않았는지 확인

---

## 📊 테스트 방법

### 통합 테스트 버튼

대시보드 → "🚀 낙상 시뮬레이션 (즉시 발동)" 버튼 클릭
→ 실제 낙상과 동일하게 전체 프로세스 실행

**테스트 시나리오:**

1. **안전 응답 테스트:** 음성이 나오면 "괜찮아"라고 말하기
   - ✅ 알림 전송 안 됨 확인
   - ✅ False Alarm으로 분류 확인

2. **긴급 응답 테스트:** "도와줘"라고 말하기
   - ✅ 즉시 텔레그램 전송 확인
   - ✅ 전화 앱 실행 확인

3. **무응답 테스트:** 아무 말도 하지 않기
   - ✅ 2차 볼륨 증가 확인
   - ✅ 최종 비상 알림 전송 확인

---

## 🎯 주요 개선 사항

### 이번 수정에서 해결된 문제들

1. ✅ **음성 시스템 안정성:** pyttsx3 → PowerShell (COM 충돌 해결)
2. ✅ **모니터 윈도우 안 뜨는 문제:** 백그라운드 마이크 초기화 블로킹 제거
3. ✅ **subprocess import 누락:** 음성 출력 안 되는 문제 해결

### 시스템 신뢰성 향상

- 모든 음성 작업은 15초 타임아웃 적용 (무한 대기 방지)
- 파일 I/O 실패 시에도 시스템 계속 작동
- 텔레그램/전화 실패 시에도 데이터는 저장됨

---

## 📝 최종 점검 체크리스트

### 설정 확인

- [ ] 대시보드 접속 (기본 비밀번호: silver1234)
- [ ] 텔레그램 봇 토큰/챗ID 입력
- [ ] 긴급 연락처 입력
- [ ] "자동 전화 걸기" 활성화
- [ ] 지역 설정 (시/도, 시/군/구)

### 테스트 확인

- [ ] 음성 출력 정상 작동
- [ ] 음성 인식 정상 작동
- [ ] 텔레그램 사진 전송 확인
- [ ] 텔레그램 영상 전송 확인
- [ ] 전화 앱 자동 실행 확인

---

## 💡 코드 유지보수 가이드

### 음성 메시지 수정

`voice_module.py` 184, 192라인의 speak() 내용 수정

### 키워드 추가

`voice_module.py` 199, 204라인의 리스트에 키워드 추가

### 알림 쿨다운 시간 변경

`core/engine.py` 81라인 `self.alert_cooldown` 값 수정 (기본 60초)

### 병원 정보 표시 개수 변경

`utils.py` 197라인 `[:3]` 값 수정 (현재 최대 3개)

---

## 🔒 보안 고려사항

- 설정 파일에 비밀번호 평문 저장 (로컬 전용)
- 텔레그램 토큰은 settings.json에 저장
- 카메라 계정 정보는 settings.json에 저장
- 외부 접근 차단 (127.0.0.1 전용)

---

**마지막 업데이트:** 2026-01-17 21:58 KST
**상태:** ✅ 모든 기능 정상 작동 확인
