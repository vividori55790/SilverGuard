# SilverGuard - AI 낙상 감지 시스템 기능 명세서

**버전**: 1.0  
**작성일**: 2026-01-17  
**목적**: 시연 영상 제작 참고용

---

## 📋 시스템 개요

SilverGuard는 **고령자 낙상 사고를 실시간으로 감지**하고, **자동으로 보호자에게 알림**을 전송하는 AI 기반 안전 모니터링 시스템입니다.

---

## 🎯 핵심 기능

### 1. 실시간 낙상 감지

| 항목     | 상세                                                |
| -------- | --------------------------------------------------- |
| **기술** | YOLOv11 Pose + ST-GCN (시공간 그래프 합성곱 신경망) |
| **입력** | 웹캠/IP카메라 영상 (30fps)                          |
| **처리** | 17개 관절 추출 → 30프레임 시퀀스 분석 → 낙상 판정   |
| **출력** | 실시간 바운딩 박스 + 상태 표시                      |

### 2. 다단계 확인 시스템

| 단계    | 동작                            |
| ------- | ------------------------------- |
| **1차** | AI 낙상 감지 (이미지/영상 저장) |
| **2차** | 음성 확인 "괜찮으십니까?" (TTS) |
| **3차** | 음성 응답 분석 (STT)            |
| **4차** | 무응답/위급 시 알림 전송        |

### 3. 자동 알림 시스템

| 기능              | 상세                                         |
| ----------------- | -------------------------------------------- |
| **텔레그램 알림** | 이미지 + 동영상 + 인근 병원 정보 전송        |
| **자동 전화**     | Windows Phone Link 연동 자동 발신            |
| **오프라인 대기** | 인터넷 끊김 시 로컬 저장 → 복구 시 자동 전송 |

### 4. 사생활 보호 모드

| 모드            | 화면 표시                            |
| --------------- | ------------------------------------ |
| **일반 모드**   | 실제 카메라 영상                     |
| **아바타 모드** | 스켈레톤 + 배경만 표시 (얼굴 비공개) |

### 5. 다중 카메라 지원

| 기능              | 상세                            |
| ----------------- | ------------------------------- |
| **로컬 웹캠**     | USB 웹캠 자동 연결              |
| **원격 카메라**   | IP Webcam 앱 / RTSP 스트림 지원 |
| **리소스 최적화** | 변화 감지 기반 추론 건너뛰기    |

### 6. 대시보드 (Web UI)

| 기능              | 상세                          |
| ----------------- | ----------------------------- |
| **설정 관리**     | 카메라, 텔레그램, 민감도 설정 |
| **사고 갤러리**   | 감지된 낙상 이미지/영상 확인  |
| **데이터 분류**   | 실제 낙상 / 오작동 라벨링     |
| **시스템 모니터** | 실시간 FPS, 리소스 사용량     |

### 7. 자동 학습 (Self-Learning)

| 기능               | 상세                              |
| ------------------ | --------------------------------- |
| **데이터 수집**    | 사용자 분류 데이터 자동 저장      |
| **자동 재학습**    | 30건 이상 + 1일 경과 시 자동 학습 |
| **모델 자동 적용** | 학습 완료 시 엔진에 즉시 반영     |

---

## 📊 정량적 성능 평가

### AI 모델 성능 (ST-GCN)

| 지표                   | 값         | 설명                            |
| ---------------------- | ---------- | ------------------------------- |
| **정확도 (Accuracy)**  | **83.74%** | 전체 샘플 중 정확히 분류한 비율 |
| **정밀도 (Precision)** | 84.18%     | 낙상 예측 중 실제 낙상 비율     |
| **재현율 (Recall)**    | 84.90%     | 실제 낙상 중 감지한 비율        |
| **F1-Score**           | 84.54%     | 정밀도와 재현율의 조화평균      |

### 혼동 행렬 (Confusion Matrix)

```
              예측: Normal    예측: Fall
실제: Normal      TN            FP (오탐)
실제: Fall        FN (미탐)     TP
```

| 구분                    | 설명            | 시스템 영향 |
| ----------------------- | --------------- | ----------- |
| **TP (True Positive)**  | 낙상 → 감지 ✓   | 정상 알림   |
| **TN (True Negative)**  | 정상 → 무시 ✓   | 정상 동작   |
| **FP (False Positive)** | 정상 → 감지 ✗   | 오알림 발생 |
| **FN (False Negative)** | 낙상 → 미감지 ✗ | 위험 (놓침) |

### 오탐 방지 로직

| 조건               | 동작                               |
| ------------------ | ---------------------------------- |
| **체형 수평 각도** | 몸통이 50° 이상 기울어야 낙상 가능 |
| **급강하 속도**    | 머리 Y 속도가 임계값 이상 필요     |
| **연속 프레임**    | 3프레임 연속 감지해야 확정         |
| **음성 확인**      | "괜찮아" 응답 시 알림 취소         |

---

## 🖥️ 리소스 최적화 성능

### CPU 사용량 절감

| 상황              | 절감률 | 설명                      |
| ----------------- | ------ | ------------------------- |
| 정적 장면 (빈 방) | 80~95% | 변화 없으면 추론 건너뛰기 |
| 저활동 (앉아있음) | 50~70% | 느린 움직임 필터링        |
| 일반 활동         | 30~50% | 검색 빈도 최적화          |
| 고활동/낙상       | 33%    | 최소 보장 (3프레임 간격)  |

### 안전 장치

- 30프레임 연속 무변화 시 **강제 확인** (쓰러진 상태 감지)
- 3프레임 연속 낙상 감지 시 **확정** (오탐 방지)

---

## 🎬 시연 시나리오

### 시나리오 1: 정상 낙상 감지

1. 사람이 카메라 앞에서 일상 활동
2. 갑자기 넘어짐 (낙상)
3. 화면에 빨간 박스 + "FALL!" 표시
4. TTS: "낙상이 감지되었습니다. 괜찮으십니까?"
5. 무응답 또는 "도와줘" 응답
6. 텔레그램으로 이미지 + 영상 + 병원 정보 전송
7. 자동 전화 발신

### 시나리오 2: 오탐 방지 (앉기)

1. 사람이 의자에 앉음
2. 잠시 주황색 표시 후 **정상으로 복귀**
3. 알림 발생하지 않음

### 시나리오 3: 사생활 보호 모드

1. 설정에서 "버추얼 모드" 활성화
2. 화면에 실제 영상 대신 **스켈레톤 아바타**만 표시
3. 낙상 감지 동일하게 동작

### 시나리오 4: 원격 카메라

1. 스마트폰에 IP Webcam 앱 설치
2. 대시보드에서 원격 카메라 주소 입력
3. PC + 스마트폰 동시 모니터링

---

## 📁 파일 구조

```
SilverGuard/
├── main.py              # 엔진 + 모니터 실행
├── dashboard.py         # Streamlit 웹 대시보드
├── core/
│   ├── engine.py        # 메인 처리 루프
│   └── detectors.py     # YOLO + ST-GCN 감지기
├── voice_module.py      # TTS/STT 음성 모듈
├── skeleton_avatar.py   # 사생활 보호 아바타
├── utils.py             # 유틸리티 함수
├── stgcn.py             # ST-GCN 모델 정의
├── offline_mode.py      # 오프라인 모드 처리
├── models/
│   ├── yolo11n-pose.pt  # YOLO Pose 모델
│   └── stgcn_fall.pth   # ST-GCN 낙상 감지 모델
└── data/
    ├── settings.json    # 사용자 설정
    ├── alert_images/    # 감지된 낙상 데이터
    ├── verified_falls/  # 분류된 실제 낙상
    └── false_alarms/    # 분류된 오작동
```

---

## 🔧 실행 방법

### 1. 엔진 실행

```bash
python main.py
```

### 2. 대시보드 실행

```bash
streamlit run dashboard.py
```

### 3. 통합 실행 (배치 파일)

```bash
Start_SilverGuard.bat
```

---

## 📌 기술 스택

| 분류          | 기술                               |
| ------------- | ---------------------------------- |
| **AI**        | PyTorch, YOLOv11, ST-GCN           |
| **영상 처리** | OpenCV, NumPy                      |
| **UI**        | Streamlit                          |
| **음성**      | pyttsx3, SpeechRecognition         |
| **알림**      | Telegram Bot API                   |
| **전화**      | Windows Phone Link (tel: protocol) |

---

_SilverGuard - 고령자 안전을 위한 AI 솔루션_
